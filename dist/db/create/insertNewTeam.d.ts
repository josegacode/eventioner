/**
 * @params An object which contains the team's
 * information and server's event data
 *
 * Saves new team built in database
 * */
export function insertNewTeam(event: any, roleName: any): Promise<void>;
